

let pattern = "#";  //Binding for holding the hashtag state.

//for iterating the printing out of the pattern.
for (let i = 0; i < 7; i++) {

  console.log(pattern); //Prints out the pattern.
  pattern += '#';       //Updates the number of hashtags in the pattern.

}
